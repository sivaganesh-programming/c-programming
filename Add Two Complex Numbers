#include <stdio.h>

struct Complex {
    int real, imag;
};

int main() {
    struct Complex c1 = {2,3}, c2 = {4,5}, sum;

    sum.real = c1.real + c2.real;
    sum.imag = c1.imag + c2.imag;

    printf("Sum = %d + %di", sum.real, sum.imag);
    return 0;
}
