#include <stdio.h>
#include <string.h>
void freq(char *str){
    int count[256]={0};
    for(int i=0;str[i];i++) count[(int)str[i]]++;
    for(int i=0;i<256;i++) if(count[i]>0) printf("%c:%d\n",i,count[i]);
}
int main(){
    char str[]="hello";
    freq(str);
    return 0;
}
