#include <stdio.h>
int main(){
    int arr[]={1,2,3,4}, n=4;
    for(int i=0;i<n;i++){
        int prod=1;
        for(int j=0;j<n;j++) if(i!=j) prod*=arr[j];
        printf("%d ",prod);
    }
    return 0;
}
