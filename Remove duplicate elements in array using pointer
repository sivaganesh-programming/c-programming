#include <stdio.h>
int main(){
    int arr[]={1,2,2,3}, n=4;
    for(int i=0;i<n;i++){
        int flag=0;
        for(int j=0;j<i;j++) if(arr[i]==arr[j]) flag=1;
        if(!flag) printf("%d ", arr[i]);
    }
    return 0;
}
