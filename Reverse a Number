#include <stdio.h>

int main() {
    int num = 1234, rev = 0, r;

    while(num > 0) {
        r = num % 10;
        rev = rev * 10 + r;
        num = num / 10;
    }

    printf("Reversed number = %d", rev);
    return 0;
}
