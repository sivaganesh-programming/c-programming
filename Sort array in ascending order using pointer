#include <stdio.h>
int main(){
    int arr[]={4,2,5,1}, *p=arr, temp;
    for(int i=0;i<4;i++)
        for(int j=i+1;j<4;j++)
            if(*(p+i)>*(p+j)){ temp=*(p+i); *(p+i)=*(p+j); *(p+j)=temp; }
    for(int i=0;i<4;i++) printf("%d ", *(p+i));
    return 0;
}
