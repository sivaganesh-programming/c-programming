#include <stdio.h>

struct Student {
    int roll;
    char name[20];
    float marks;
};

int main() {
    struct Student s = {1, "Ram", 85.5};

    printf("Roll=%d\nName=%s\nMarks=%.1f", s.roll, s.name, s.marks);
    return 0;
}
