#include <stdio.h>

struct Student {
    int roll;
    float marks;
};

int main() {
    struct Student s1;   // Object creation

    s1.roll = 1;
    s1.marks = 85.5;

    printf("Roll = %d\n", s1.roll);
    printf("Marks = %.1f", s1.marks);

    return 0;
}
