#include <stdio.h>

struct Bank {
    int accNo;
    float balance;
};

int main() {
    struct Bank b = {101, 5000};

    b.balance += 1000;   // deposit

    printf("New Balance = %.2f", b.balance);
    return 0;
}
