#include <stdio.h>
void main() {
 int num, i, count = 0;
 scanf("%d", &num);
 for (i = 1; i <= num; i++) {
 if (num % i == 0) {
 count++; 
 }
 }
 printf("Number of factors of %d is %d\n", num, count);
}
